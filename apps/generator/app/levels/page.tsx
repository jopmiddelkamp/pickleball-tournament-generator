import { LevelGuide } from "../../components/LevelGuide";

/** Public and static in content: linked from the sign-up form for anyone unsure of their level. */
export default function LevelsPage() {
  return <LevelGuide />;
}
